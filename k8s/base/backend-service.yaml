apiVersion: v1
kind: Service
metadata:
  name: learntogrow-backend
  namespace: default
spec:
  selector:
    app: learntogrow-backend
  ports:
    - port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
