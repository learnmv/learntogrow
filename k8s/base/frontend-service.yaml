apiVersion: v1
kind: Service
metadata:
  name: learntogrow-frontend
  namespace: default
spec:
  selector:
    app: learntogrow-frontend
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
