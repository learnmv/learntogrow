apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: default
data:
  config.json: |
    {
      "apiUrl": "http://10.0.0.131:30800/api/v1",
      "appName": "LearnTorow Dev",
      "environment": "development",
      "features": {
        "enableDebug": true,
        "showDebugInfo": true
      },
      "defaults": {
        "questionType": "multiple_choice",
        "difficulty": 0.5
      }
    }
